import { MantineSpacing, MantineStyleProps, StyleProp } from "@mantine/core";

export type PropAdminShellHeader = {
  pl?: StyleProp<MantineSpacing> | undefined;
  headerActions?: React.ReactNode;
};
